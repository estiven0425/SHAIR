const buttom_login = document.getElementById('login'); //llamamos etiqueta en una variable

buttom_login.addEventListener('click', function() { //Variable tiene un evento click con la función
  window.location.href = './new_creator.php'; //Buscar en la carpeta
});