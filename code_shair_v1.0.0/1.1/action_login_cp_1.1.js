const buttom_cp = document.getElementById('cp'); //llamamos etiqueta en una variable

buttom_cp.addEventListener('click', function() { //Variable tiene un evento click con la función
  window.location.href = './index_cp.php'; //Buscar en la carpeta
});