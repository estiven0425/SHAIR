import React from 'react' // Importamos la librería de React para crear componentes
import './TextoSeccion3.css'; // Importamos el archivo de estilos CSS para el componente

export default function TextoSeccion3() { // Exportamos una función llamada TextoSeccion3 que retorna un elemento JSX
  return ( // Retornamos el contenido JSX del componente
    <div id='TextoSeccion3'> {/* Creamos un div con id TextoSeccion3 que contiene un elemento p */}
        <p>Aqui no tienes fronteras</p> {/* Creamos un elemento p que renderiza un texto con el mensaje "Aqui no tienes fronteras" */}
    </div>
  )
}
