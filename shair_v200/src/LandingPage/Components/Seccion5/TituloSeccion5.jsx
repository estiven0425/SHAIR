import React from 'react' // Importamos la librería de React para crear componentes
import './TituloSeccion5.css'; // Importamos el archivo de estilos CSS para el componente

export default function TituloSeccion5() { // Exportamos una función llamada TituloSeccion5 que retorna un elemento JSX
  return ( // Retornamos el contenido JSX del componente
    <div id='TituloSeccion5'> {/* Creamos un div con id TituloSeccion5 que contiene un elemento h1 */}
        <h1>Últimos detalles</h1> {/* Creamos un elemento h1 que renderiza un texto con la pregunta "Últimos detalles" */}
    </div>
  )
}
